FROM node:20-alpine

WORKDIR /app

# Expose port
EXPOSE 3000

# Run development server as root for development (simpler permissions) with request logging
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--logLevel", "info", "--clearScreen", "false"]